# -----------------------------------------------------------------------------------------------------------------------------------
# names: gary young, colin chu
# program name (official): calculatormodified.kv
# program name (unofficial): partial connectfour setup, on the clients end
# purpose of the program: to setup part of the GUI for calculatormodified.py, so some stuff in objects don't have to be repeated
#                         such as Image(source='yellow.png',fit_mode='scale_down',size_hint=[1,1/6],color=[1,1,1,app.opacity])
#                         which would be tedious to constantly keep writing - so you just call Yellow() instead that initializes
                          what is desired in this file
# We pledge
# -----------------------------------------------------------------------------------------------------------------------------------

<Red@Image>:
  source: 'red.png'
  fit_mode: 'scale-down'
  size_hint: [1, 1/6]
  color: [1,1,1,app.opacity]

<Yellow@Image>
  source: 'yellow.png'
  fit_mode: 'scale-down'
  size_hint: [1, 1/6]
  color: [1,1,1,app.opacity]

<EmptyStackLayout@StackLayout>:
  orientation: 'bt-lr'

<ColumnButton@Button>:
  background_color: (144/88,238/88,144/88,1)
  background_disabled_normal: 'light_red.png'
  markup: True